{
  "devDependencies": {
    "concurrently": "^9.2.1",
    "prettier": "^3.8.3"
  },
  "scripts": {
    "start": "concurrently \"cd frontend && yarn dev\" \"cd server && yarn start\"",
    "format": "yarn --cwd server format"
  }
}
